安装
===========

- Python >= 3.8
- Pytorch >=1.11

可以依据 `此链接 <https://github.com/pytorch/pytorch#installation>`_ 安装Pytorch。

然后, 您可以使用如下 ``pip`` 指令安装 **DHG** 包：

.. code-block:: bash

    pip install dhg
